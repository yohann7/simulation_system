optimization_algorithm 模块 —— 工艺优化方案

═══════════════════════════════════════════════════════════════
一、现状分析
═══════════════════════════════════════════════════════════════

当前 optimization_algorithm/ 文件夹有 3 个文件：

  AOA.py          算术优化算法 (AOA) + 23 个标准测试函数 (F1-F23 基准函数)
  PO.py           美洲豹优化算法 (Puma) + 与仿真系统的接口
  optimization.py 工艺参数随机生成器 + 空的 GA/PSO 桩函数

PO.py 当前方案：
  - 搜索空间 = 25 维：[C, Si, Mn, P, S, Cr, Ni, Cu, ORT, SPEED1~10, FAN1~6]
  - 评估流程：工艺参数 → run_all_simulations() → resample_sim_data() → predict_Ts()
  - 优化目标：最大化 TS（代价 = -TS）
  - 算法：Puma，种群 100，迭代 300

现存的 9 个问题：
  1. 元素成分不是自由变量（上下界相同=自废维度）
  2. 两级代理模型误差叠加（sim_T 误差 + ML 误差 → 虚假最优）
  3. 代价函数评估昂贵（数万次 sim_T 仿真）
  4. 批量评估中 predict_Ts 逐条调用，GPU 利用率低
  5. F1-F23 测试函数与项目无关
  6. 缺少物理约束（冷却速率、速度跳跃等）
  7. 目标函数过于单一（仅最大化 TS，忽略塑性和组织）
  8. optimization.py 是半成品
  9. AOA.py 是完全独立的通用优化库，与仿真系统无连接

1.4 生产数据集字段说明（工艺数据全.xlsx，219 条）

  工艺参数列（25 列，作为优化搜索空间）：
    MAT_NO          生产批号
    PRODUCT_TIME    生产时间
    C_ELE ~ CU_ELE  元素成分（C, Si, Mn, P, S, Cr, Ni, Cu），单位 wt%
    ORT             吐丝温度 (°C)，范围 883–899
    SPEED0 ~ 10     各段辊道速度 (m/s)
    FAN1 ~ 7        各段风机开度 (%)

  力学性能列：
    EXT    断面收缩率 Z (%)，范围 31–51，均值 39.9
            82A 帘线钢标准 Z ≥ 38%，是拉拔性能最关键的指标
    TS     抗拉强度 (MPa)，范围 964–1246，均值 1149
            标准要求 1050 ± 70 MPa（980–1120），均值超上限

  仿真微观组织列（来自 sim_T 的相变计算输出）：
    V_COOL     珠光体温区平均冷却速率 (°C/s)，范围 4.2–19.5，均值 16.0
                与 spacing 的相关系数 r = -0.978（冷却越快→片层越细）
    spacing    珠光体片层间距 S₀ (μm)，范围 0.138–0.197，均值 0.149
                与 TS 的 r = -0.455（片层细→强度高）
    Cp         spacing 的线性变换 (= spacing / 0.02858)，与 spacing 的 r = 0.999
                信息完全等价，建模时二选一即可
    Rp         珠光体强度贡献项，范围 34.6–94.0，均值 87.7
                Rp × spacing ≈ 13.0（常数，变异仅 4.6%），即 Rp ∝ 1/spacing
                不是常规屈服强度（82A 的 Rp0.2 典型值 700–900 MPa）
    phase      先共析相类型：Ferrite(铁素体，177条) / Cementite(渗碳体，42条)
                sim_T 根据成分和冷却条件预测
    fraction   先共析相体积分数 (%)，范围 0.01–2.59，均值 0.76
                分数↑ → EXT↑ (r=0.32)、TS↓ (r=-0.58)

  成分衍生列：
    Mole_C     C 原子分数 (at%)，约 4.8 at%，与 C_ELE 的 r=0.70
    Mole_Mn    Mn 原子分数 (at%)，与 MN_ELE 的 r=0.93
    Mole_Si    Si 原子分数 (at%)，与 SI_ELE 的 r=0.997
    C_eq       碳当量 = C + Mn/6 + Si/24，范围 0.90–0.97，均值 0.92

  关键发现：
    1. Cp 是 spacing 的冗余列，两者 r=0.999，二选一即可
    2. Rp 非屈服强度，是珠光体片层间距的强度贡献分量
    3. EXT 列名虽叫 Elongation，实际是断面收缩率 Z（值域 31–51% 不可能是 A）
    4. 数据集包含三个指标：Z(EXT) + TS + 微观组织仿真结果，可用于综合质量评估
    5. Cementite 组：低 fraction(均值0.036)、高 TS(均值1200)、低 EXT(均值37)、高 C_eq(均值0.959)
       Ferrite 组： 高 fraction(均值0.93)、中 TS(均值1137)、高 EXT(均值41)、低 C_eq(均值0.914)
    6. 高 EXT 样本特征：低 C_eq、高 fraction、粗 spacing、慢 V_COOL
       低 EXT 样本特征：高 C_eq、低 fraction、细 spacing、快 V_COOL（有马氏体风险）

═══════════════════════════════════════════════════════════════
二、总体方案：专家知识指导 + 性能调优（灰箱优化）
═══════════════════════════════════════════════════════════════

采用三步法，将纯黑箱优化升级为物理约束 + 数据驱动的灰箱优化。

  Step 1: 总结专家知识 → 转化为约束条件
  Step 2: 分析工艺参数 → 力学性能的相关性，量化影响
  Step 3: 构建评分方法（约束 + 工艺-性能关系）→ 用优化算法求解

两阶段实施策略：
  Phase 1: 用 change_parameter_82A.py 修正 sim_T 的仿真精度
  Phase 2: 用 optimization_algorithm 在修正后的 sim_T 上优化工艺参数

═══════════════════════════════════════════════════════════════
三、Step 1：专家知识 → 约束条件（文献调研成果）
═══════════════════════════════════════════════════════════════

3.1 82A 牌号与化学成分标准

  3.1.1 牌号对照

    82A（SWRH82A）源自日本标准 JIS G 3506《高碳钢盘条》。不同标准体系下对应牌号：
      JIS G 3506:         SWRH82A / SWRH82B（仅 Mn 含量不同）
      GB/T 27691-2017:    LX80A / LX80B（对应 80 级帘线钢）
      T/JSGT 006-2021:    LX82A（钢帘线用）/ C82DA（胎圈钢丝用）
      KS D 3559:          HSWR82A/HSWR82B

    牌号规则：LX = "帘线"拼音，82 = C 含量下限 0.82 wt%，A/B = 质量等级（Mn 差异）

  3.1.2 化学成分范围（JIS G 3506 SWRH82A）

    ┌────────┬────────────────┬──────────────────────────────────┐
    │ 元素   │ 标准范围 (wt%) │ 典型实际值                       │
    ├────────┼────────────────┼──────────────────────────────────┤
    │ C      │ 0.79 ~ 0.86    │ 0.81                             │
    │ Si     │ 0.15 ~ 0.35    │ 0.25                             │
    │ Mn     │ 0.30 ~ 0.60    │ 0.54                             │
    │ P      │ ≤ 0.030        │ 0.011                            │
    │ S      │ ≤ 0.030        │ 0.006                            │
    │ Cr     │ ≤ 0.20 (残余)  │ 0.19                             │
    │ Ni     │ ≤ 0.25 (残余)  │ —                                │
    │ Cu     │ ≤ 0.25 (残余)  │ —                                │
    │ Cr+Ni+Cu│ ≤ 0.25 (合计) │ — (国标要求)                     │
    └────────┴────────────────┴──────────────────────────────────┘

    82A vs 82B 唯一区别：82B 的 Mn = 0.60-0.90 wt%（更高淬透性，用于预应力钢丝/钢绞线）。
    Cr 在帘线钢中不是有意添加元素，但实际常有微量（~0.15-0.25%），具有细化珠光体片层间距的作用。

  3.1.3 成分对组织性能的影响（冶金原理）

    - C ↑ → 珠光体量 ↑、片层间距 ↓ → TS ↑、EXT ↓
    - Mn ↑ → 奥氏体稳定化 → CCT 曲线右移 → 临界冷速 ↓（淬透性 ↑）
    - Si ↑ → 固溶强化 → TS ↑；但过高促进石墨化
    - Cr ↑ → 显著细化珠光体片层间距 → TS ↑↑；推迟珠光体转变，增加马氏体风险
    - P, S → 有害元素，增加偏析和夹杂物 → 越低越好

3.2 临界相变温度（82A/82B 实测数据）

  3.2.1 平衡临界温度

    82A 与 82B 碳含量几乎相同，临界温度可互用。基于 82B 的 Gleeble 实测数据
    （YB/T 5127-2018 膨胀法，≤ 200 °C/h）：

    ┌──────────────┬──────────┬──────────────────────────────────┐
    │ 临界点       │ 温度 (°C)│ 物理意义                         │
    ├──────────────┼──────────┼──────────────────────────────────┤
    │ Ac₁          │ 731 ~ 735│ 加热时珠光体 → 奥氏体开始       │
    │ Accm         │ 780 ~ 790│ 加热时二次渗碳体全溶入奥氏体     │
    │ Arcm         │ ~750     │ 冷却时先共析渗碳体开始析出       │
    │ Ar₁          │ ~690-700 │ 冷却时奥氏体 → 珠光体开始       │
    │ Ms           │ ~250     │ 马氏体开始转变（估算）            │
    └──────────────┴──────────┴──────────────────────────────────┘

    补充：C82DA（对应 LX82A）的实测值（刘力哲等，2014）：
      Ac₁ = 732 °C, Accm = 759 °C

  3.2.2 CCT 连续冷却转变规律（82A WLX82A，Gleeble 1500 实测）

    实验条件：1100 °C × 5 min → 冷至 1050 °C → 变形 60%（应变速率 50/s）
    → 以 880/910/940 °C 为起始冷却温度，分别以 0.8-40 °C/s 冷却。

    关键发现：
      - 同一冷却速率下，起始冷却温度越高 → 相变终了温度越高
      - 冷却速率 ↑ → 相变开始温度 ↓ → 过冷度 ↑ → 珠光体片层间距 ↓
      - 冷却速率 ↑ → 铁素体形核率 ↑↑ → 先共析铁素体细化、分数减少

  3.2.3 不同冷却速率下的珠光体转变区间（82B CCT 数据，可代表 82A）

    ┌────────────┬─────────────────┬──────────────────┬──────────────┐
    │ 冷却速率   │ 珠光体开始 (°C) │ 珠光体结束 (°C)  │ 组织特征     │
    ├────────────┼─────────────────┼──────────────────┼──────────────┤
    │ 0.1-1 °C/s │ ~680-650        │ ~620-600         │ 粗珠光体     │
    │ 1-3 °C/s   │ ~650-620        │ ~580-550         │ 细珠光体     │
    │ 3-5 °C/s   │ ~620-580        │ ~550-500         │ 极细珠光体   │
    │ > 5 °C/s   │ —               │ —                │ 出现马氏体！ │
    │ > 30 °C/s  │ —               │ —                │ 马氏体为主   │
    └────────────┴─────────────────┴──────────────────┴──────────────┘

3.3 JMAK 相变动力学参数（C82DA / LX82A）

  来源：刘力哲等，《C82DA帘线钢相变规律及动力学模型》，材料热处理学报，2014, 35(2): 220-224

    Avrami 方程：X = 1 - exp(-k · tⁿ)
      n = 1.8214（Avrami 指数，恒定）
      ln k = -1.61121×10⁻⁴ · T² + 0.17083 · T - 45.56905
      （T 为温度 °C，适用于 550-680 °C 珠光体温区）

    注意：该文献 lnk-T 关系为二次多项式，未直接给出激活能 Q。
    若需 Arrhenius 形式 (k = k₀·exp(-Q/RT))，需从实验数据另行拟合。

3.4 82A 帘线钢力学性能标准

  3.4.1 T/JSGT 006-2021《胎圈钢丝及钢帘线用盘条》— LX82A

    ┌──────────────────────┬───────────────────────┐
    │ 指标                 │ 要求                  │
    ├──────────────────────┼───────────────────────┤
    │ 抗拉强度 TS          │ 1050 ± 70 MPa         │
    │                      │ （即 980 ~ 1120 MPa） │
    │ 断面收缩率 Z         │ ≥ 38%                 │
    │ 索氏体含量           │ ≥ 85%                 │
    │ P                    │ ≤ 0.012%              │
    │ S                    │ ≤ 0.010%              │
    │ Cr + Ni + Cu         │ ≤ 0.15%（合计）       │
    │ 非金属夹杂物         │ 符合标准规定          │
    │ 脱碳层深度           │ ≤ 1.0%D              │
    │ 中心偏析             │ ≤ 1.0 级             │
    │ 网状渗碳体           │ ≤ 1.0 级             │
    │ 不得有马氏体         │ 硬约束                │
    └──────────────────────┴───────────────────────┘

    注：力学性能为自然时效 15 天或人工时效（120 °C × 10 h）后测量，仲裁以 28 天自然时效为准。

  3.4.2 GB/T 27691-2017《钢帘线用盘条》— LX80A/LX80B（可参考）

    - 抗拉强度：1070 ~ 1220 MPa
    - 断面收缩率：标准规定值（略低于 T/JSGT 006）
    - 索氏体含量：以索氏体为主（量化表3），≥ 85%（实际执行）

  3.4.3 实际生产典型值（SWRH82A+Cr，Φ5.5mm）

    来源：《SWRH82A+Cr热轧盘条生产工艺实践》，金属制品，2015

    ┌──────────────────┬────────────────┐
    │ 指标             │ 典型值         │
    ├──────────────────┼────────────────┤
    │ 抗拉强度         │ 1196-1221 MPa  │
    │ 断面收缩率       │ 39-42%         │
    │ 断后伸长率       │ 13.5-16.5%     │
    │ 索氏体体积分数   │ ≥ 85%          │
    │ 晶粒度           │ 8.0 级         │
    └──────────────────┴────────────────┘

3.5 理想冷却曲线 —— 82A 帘线钢斯太尔摩风冷

  3.5.1 核心工艺参数（汇总多个来源）

    来源 A：82A 钢高速线材控制冷却工艺（Mysteel，WLX82A 的 CCT 实测）
    来源 B：82A 控冷工艺优化（王伯文、杭乃勤，特殊钢，2009）
    来源 C：Li Zhanwei et al. (2023) LX82ACr 定量 CCT
    来源 D：SWRH82B 有限元模拟与参数优化（Deng et al., 2025）

    ┌────────────────────┬─────────────────┬──────────────────────┐
    │ 参数               │ 推荐值          │ 来源                 │
    ├────────────────────┼─────────────────┼──────────────────────┤
    │ 吐丝温度 (ORT)     │ 910-930 °C      │ A, B (WLX82A CCT)   │
    │ 吐丝温度波动       │ ≤ ±10 °C        │ A                    │
    │ 相变目标温度       │ ~630 °C         │ A, B (索氏体区)     │
    │ 相变阶段冷却速率   │ 9-12 °C/s       │ A (WLX82A 推荐)     │
    │ 相变前冷速         │ 20-30 °C/s      │ A (快速过奥氏体区)  │
    │ 相变后冷速         │ 1-3 °C/s        │ C, D (避免过冷)     │
    │ 风机相变完成段位   │ 2#开始, 4#前完成│ A, B                │
    │ 1#-2# 风机开度     │ 100% (全开)     │ A                    │
    │ 3# 风机开度        │ 85-100%         │ A                    │
    │ 4#-10# 风机开度    │ 适当降低        │ A                    │
    │ 前段辊道速度       │ 0.4-0.6 m/s     │ A                    │
    │ 最佳均匀性辊速     │ 0.70-0.75 m/s   │ D (搭接温差最小)    │
    │ 前四段速比         │ 1.10→1.15→1.20→1.15 │ A              │
    │ 保温罩 1-2#        │ 关闭            │ A (推迟相变)        │
    └────────────────────┴─────────────────┴──────────────────────┘

  3.5.2 三段式理想冷却曲线（82A 帘线钢，基于上述文献合成）

    阶段 1 — 快速冷却段（吐丝 → Ac₁，辊道 0 ~ 约 20m，时间 0 ~ 约 2-5s）:
      温度区间：920 °C → 730 °C
      冷却速率：~20-30 °C/s（较大风量 + 较快辊速）
      风机策略：1# 2# 全开 (100%)
      物理目的：快速通过奥氏体单相区，抑制先共析渗碳体沿晶界网状析出。
                Ac₁ (731°C) 以上为奥氏体单相区，快速通过可防止晶界碳化物形核。
      关键约束：冷却不能慢于 10 °C/s，否则网状渗碳体风险显著增加。

    阶段 2 — 相变控温段（Ac₁ → 相变完成，辊道 20m → 约 60m，时间 ~5-25s）:
      温度区间：730 °C → 630 °C → ~550 °C
      冷却速率：~8-12 °C/s（珠光体转变阶段，精细控冷）
      风机策略：3# 85-100%，4#-6# 根据温度反馈调节
      物理目的：在 ~630 °C 附近实现近等温珠光体转变，获得均匀细小的索氏体组织。
                使珠光体片层间距 S₀ ≈ 0.12-0.16 μm，平衡强度与塑性。
      关键约束：冷却速率 ∈ [8, 15] °C/s（硬约束）。温度不能低于 550 °C。

    阶段 3 — 缓冷段（相变完成 → 集卷，辊道 60m → 尾端，时间 ~25-80s）:
      温度区间：550 °C → 500 °C → ~350-400 °C（集卷温度）
      冷却速率：~1-3 °C/s（缓冷，关闭/部分开启保温罩）
      风机策略：7#-10# 低开度或关闭
      物理目的：完成残余奥氏体转变，避免冷却过快产生热应力/马氏体/贝氏体。
                珠光体转变在 ~550 °C 以下基本完成。
      关键约束：冷却速率 < 5 °C/s，温度不能进入 Ms (~250 °C) 以上 50 °C 范围。

  3.5.3 珠光体片层间距与冷却速率的定量关系（LX82ACr, 2023）

    基于 Li Zhanwei et al. (2023) 的 LX82ACr CCT 研究（与 82A 可互参）：

    ┌──────────────┬─────────────────┬────────────────┐
    │ 冷却速率     │ 片层间距 S₀ (μm)│ 抗拉强度 (MPa) │
    ├──────────────┼─────────────────┼────────────────┤
    │ 0.1 °C/s     │ 0.193           │ 993            │
    │ 3 °C/s       │ ~0.15           │ ~1100          │
    │ 9 °C/s       │ 0.074           │ 1388            │
    └──────────────┴─────────────────┴────────────────┘

    拟合关系（R² > 0.97）：TS 与 ln(CR) 近似线性；S₀ 与 ln(CR) 近似线性。
    82A 目标 TS = 1050 ± 70 MPa → S₀ ≈ 0.12-0.17 μm → CR ≈ 3-10 °C/s

    注意：82A 的 TS 有上下限，并非越大越好！TS > 1120 MPa → EXT 可能偏低。

3.6 搭接/非搭接温差与热均匀性（佳灵装置 Optiflex）

  3.6.1 温差来源

    吐丝成圈后线环在辊道上形成密集搭接区（两侧堆积）和疏松非搭接区（中间）。
    - 搭接点：堆积厚密 → 换热系数小 → 冷却慢 → 温度高
    - 非搭接点：堆积疏松 → 换热系数大 → 冷却快 → 温度低
    - 同一截面上表面换热系数相差 70-100 W/(m²·K)
    - 最大温差可达 30-50 °C（无措施时）

  3.6.2 佳灵装置（Optiflex）工作原理

    调节辊道横向风量分配，使两侧搭接点风量大、中间风量小，实现均匀冷却。
    - 优化方案（Deng et al., 2025）：前 4 台风机 3° 开启角，后 4 台 1.5°
    - 优化效果：搭接/非搭接表面温差可控制在 6 °C 以内

  3.6.3 辊道速度与温差的关系

    - 最优辊速范围：0.70-0.75 m/s，温差最小
    - 对应线圈间距：73.90-79.18 mm
    - 辊速过低→搭接过密；辊速过高→线圈间距过大
    - 辊速交替变化（慢→快→慢→快，幅度 15-20%）可分散搭接点位置

  3.6.4 搭接/非搭接温差约束目标

    - 全程 max ΔT ≤ 20 °C（可接受范围）
    - 相变区（630 °C 附近）max ΔT ≤ 15 °C（严格范围）
    - 理想目标（经 Optiflex 优化）：≤ 6 °C
    - 约束类型：软约束（超过惩罚，非硬拒绝）

3.7 异常组织临界条件与控制

  3.7.1 马氏体

    - 临界冷速（82B，不变形）：5 °C/s（超过即可能出现马氏体）
    - 临界冷速（82B，一次热变形）：10 °C/s
    - 临界冷速（82B，两次热变形）：12 °C/s
    - 热变形使 CCT 曲线左移（促进珠光体也促进马氏体）
    - Ms ≈ 250 °C（按 Steven-Haynes 公式估算）
    - 82A 帘线钢绝对禁止出现马氏体（硬约束）

  3.7.2 贝氏体

    - SWRS82B（含 V）：温度 < 550 °C + 冷速 ≥ 3 °C/s → 贝氏体
    - 贝氏体为不希望出现的异常组织
    - 约束：550 °C 以下冷却速率 ≤ 5 °C/s

  3.7.3 网状渗碳体（先共析渗碳体 / 晶界渗碳体 GBC）

    - 形成条件：冷速 ≤ 0.5 °C/s（过慢），碳在奥氏体晶界处有充分时间扩散偏聚
    - 82A（C ≈ 0.80%）：冷速 ≥ 1 °C/s 即可显著抑制网状渗碳体
    - 82A 避免马氏体的临界冷速 < 25 °C/s（含 Cr 时更严）
    - 标准要求：网状渗碳体 ≤ 1.0 级
    - 约束：先共析渗碳体分数 ≤ 3%（软约束），且相变前（Ac₁ 以上）冷却速率 ≥ 10 °C/s

  3.7.4 芯部马氏体（偏析导致）

    - Cr 和 Mn 的中心偏析 → 局部 CCT 曲线右移 → 芯部易出现岛状马氏体
    - 消除方法：加热至 1200 °C + 保温 > 1h（工业生产中难以实现）
    - 对优化的启示：在满足 TS 的前提下，Mn/Cr 尽量取标准下限

3.8 设备与操作约束

  3.8.1 辊道速度约束

    ┌──────────────────────┬───────────┬──────────────────────────┐
    │ 约束类型             │ 范围      │ 依据                     │
    ├──────────────────────┼───────────┼──────────────────────────┤
    │ 辊道速度绝对范围     │ 0.05-1.30 │ Stelmor 设备能力         │
    │ 前段推荐速度         │ 0.40-0.60 │ 82A 文献推荐             │
    │ 最佳均匀性速度       │ 0.70-0.75 │ Deng et al. (2025)       │
    │ 相邻段速比上限       │ 0.85-1.25 │ 机械限制（避免速度跳跃）  │
    │ 速比推荐范围         │ 1.05-1.20 │ 前四段递增速比           │
    └──────────────────────┴───────────┴──────────────────────────┘

  3.8.2 风机开度约束

    ┌──────────────────────┬───────────┬──────────────────────────┐
    │ 约束类型             │ 范围      │ 依据                     │
    ├──────────────────────┼───────────┼──────────────────────────┤
    │ 风机开度范围         │ 0-100%    │ 设备能力                 │
    │ 实际操作精度         │ 10% 步进  │ 工业实践（离散档位）     │
    │ 相邻风机开度差       │ ≤ 40%     │ 避免气流急剧变化         │
    │ 1#-3# 最小开度       │ ≥ 50%     │ 前段必须强制冷却         │
    │ 单风机最大风量       │ 约 10 m/s │ 线环表面风速             │
    └──────────────────────┴──────────┴──────────────────────────┘

  3.8.3 其他操作约束

    - 吐丝温度波动 ≤ ±10 °C（影响通条性）
    - 环境温度/湿度季节性变化影响冷却速度（夏天 vs 冬天差异显著）
    - 风机开度与冷却速度无线性关系（受环境温度影响，需在线测温反馈）

3.9 专家知识 → 约束条件（v3 纯惩罚系统 —— 2026-05-17 重构）

  设计原则：
    1. 归一化偏差 d = 偏离专家知识边界的比例。d=1.0 表示越界100%（如冷速是阈值2倍）
    2. 分级惩罚函数：T1 用 exp(k·d)-1（硬约束软化），T2/T3 用 d²（软约束）
    3. 纯惩罚评分：Score = penalty（无奖励项，无 W_BONUS）
    4. 硬约束软化：一票否决(inf) → exp 指数连续惩罚（保留梯度信息）

  当前 TIER_CFG：k=2.0, W_T1=1.0, W_T2=0.8, W_T3=0.5

  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │        T1 致命级 —— exp(2.0×d)−1 惩罚，W=1.0                                                       │
  ├────────┬─────────────────────────────────────┬──────────────────────────────────────────────────────┤
  │ H2     │ 珠光体相变区冷速 [9, 12] °C/s       │ d = max(0, 9-cr)/9 或 (cr-12)/12                    │
  │        │ CCT曲线：索氏体最优窗口，太慢粗片    │ 取相变段单段平均冷速                                │
  │        │ 层(低强度)，太快贝氏体/马氏体        │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ H4     │ 珠光体转变量 ≥ 85%                  │ d = max(0, 0.85-pearl)/0.85                          │
  │        │ T/JSGT 006 索氏体含量标准            │ **原一票否决(inf)，现已软化**                        │
  │        │ <85% → 残余奥氏体→马氏体脆断        │ 例: pearl=0→d=1.0→pen=6.39                           │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S9     │ 相变前冷速 [20, 30] °C/s（逐段）    │ d = max(0, 20-cr)/20 或 (cr-30)/30                   │
  │        │ 过冷奥氏体区：快速过以抑制晶粒长大   │ 取相变段之前所有辊道段，逐段独立exp惩罚              │
  │        │ 和先共析渗碳体析出                  │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S5a    │ 辊速下降 ≤ 5%（防倒插料）            │ ratio=s[i]/s[i-1]; 若<1: d=max(0,1-ratio-0.05)/0.05 │
  │        │ 降速→后圈追前圈→堆叠报废            │ 非对称：降速T1 exp，容忍仅5%                         │
  ├────────┴─────────────────────────────────────┴──────────────────────────────────────────────────────┤
  │        T2 关键级 —— d² 惩罚，W=0.8                                                                    │
  ├────────┬─────────────────────────────────────┬──────────────────────────────────────────────────────┤
  │ H3     │ 相变后冷速 < 5 °C/s（逐段）          │ d = max(0, cr-5)/5                                   │
  │        │ CCT马氏体临界线，防马氏体             │ 取相变完成段后、T降至Bs前所有段，逐段d²              │
  │        │ 用d²(非exp)因相变后首段冷速自然偏高  │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S4     │ 先共析渗碳体分数 < 1.2%             │ d = max(0, cem-0.012)/0.012                          │
  │        │ 过共析钢(C>0.77%)晶界碳化物网络      │ cem = (C%-0.77)/5.9 (当铁素体<1%时)                  │
  │        │ 严重降低拉拔性能                    │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S13    │ 低温区冷速 < 5 °C/s（逐段）          │ d = max(0, cr-5)/5                                   │
  │        │ T<Bs后进入马氏体敏感区               │ 取温度降至Bs之后所有辊道段，逐段d²                    │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S5b    │ 辊速上升 ≤ 20%（防张力过大）          │ ratio>1: d=max(0, ratio-1-0.20)/0.20                  │
  │        │ 升速拉开间距→有益冷却均匀性          │ 非对称：升速T2 d²，容忍20%（专利建议15-20%）         │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S1     │ 相变区搭接/非搭接温差 < 15°C         │ d = max(0, dT_pearl-15)/15                            │
  │        │ 同圈冷却不均→性能不均匀              │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S2     │ 全程搭接/非搭接温差 < 20°C           │ d = max(0, dT_total-20)/20                            │
  │        │ 全程冷却均匀性                      │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S10    │ 仿真总时间 [75, 85] s（双向）         │ d = max(0, 75-t)/75 或 (t-85)/85                      │
  │        │ 平衡冷却充分性与生产效率             │                                                      │
  ├────────┴─────────────────────────────────────┴──────────────────────────────────────────────────────┤
  │        T3 辅助级 —— d² 惩罚，W=0.5                                                                    │
  ├────────┬─────────────────────────────────────┬──────────────────────────────────────────────────────┤
  │ S11    │ 集卷温度 < 400°C                     │ d = max(0, T_end-400)/400                             │
  │        │ 相变完成+操作安全                   │                                                      │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S12    │ 速度偏离BEST参考模式（逐段）          │ d = max(0, |speed−ref|−0.08)/0.08                      │
  │        │ BEST数据递增速度模式经实践验证       │ 10段逐段d²求和。ref: [1.17,1.27,...,1.56]             │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ S14    │ 吐丝温度偏离910°C最优值              │ d = |ort-910|/20                                      │
  │        │ 82A推荐910-930°C，910最优            │ ort=910→d=0; ort=890/930→d=1.0                        │
  └────────┴─────────────────────────────────────┴──────────────────────────────────────────────────────┘

  已删除的约束：
    - H1/H6: ORT/SPEED范围 → PO搜索边界已覆盖
    - H7: 速比硬顶1.5 → 合并入S5a（exp惩罚自然压制极端降速）
    - S7/S8: TS/Z约束 → 依赖ML预测，数据不完全
    - B1/B2/B4: 奖励项 → 转为纯惩罚系统
    - B5: 速度模式奖励 → 转为S12惩罚
    - B6: ORT奖励 → 转为S14惩罚
    - W_BONUS: 奖励权重 → 无奖励项后废弃

  约束总数：14个（T1×4 + T2×7 + T3×3），仅需标定4个参数（k, W_T1, W_T2, W_T3）
  原系统：20+约束/奖励，12+独立乘数

3.10 搜索空间最终定义（21 维，不含成分）

  固定参数（不优化，取 82A 标准中值）：
    C = 0.82, Si = 0.25, Mn = 0.50, P = 0.012, S = 0.010,
    Cr = 0.20, Ni = 0.05, Cu = 0.05

  优化变量（21 维 = ORT + SPEED1~10 + FAN1~10）：
    ┌──────────┬─────────┬───────┬──────┬──────────────────────────┐
    │ 变量     │ 下界    │ 上界  │ 步长 │ 说明                     │
    ├──────────┼─────────┼───────┼──────┼──────────────────────────┤
    │ ORT      │ 890     │ 940   │ 连续 │ °C，文献 910-930 ±10    │
    │ SPEED1~10│ 0.50    │ 1.50  │ 0.01 │ m/s，产能下限+设备能力    │
    │ FAN1~10  │ 0       │ 100   │ 连续 │ %，风机开度              │
    └──────────┴─────────┴───────┴──────┴──────────────────────────┘

    注：SPEED0 生产数据全为 NaN（吐丝机速度不纳入辊道速度序列），
        已从搜索空间中移除。
        FAN1~10 映射到 sim_T 辊道 rolls[1]~rolls[10]。
        FAN7~10 生产数据未采集（best_process_data.txt 中为 None），
        约束系数标定无法为 FAN7~10 提供指导，但仍作为优化搜索维度。
        SPEED 下界 0.5 基于产能要求，上界 1.5 基于设备最大能力，
        步长 0.01 为实际操作精度（_repair_speeds 圆整）。

3.11 约束编码实现（v3 纯惩罚系统，与 expert_constraints.py 一致）

  评价流程分两层，总分公式：
    Score = penalty（纯惩罚，最小化）

  ═══════════════════════════════════════════════════
  Level 1: 预检查 pre_check_bounds(params) — 不跑仿真
  ═══════════════════════════════════════════════════

  def pre_check_bounds(params):
      """仅检查非对称速比约束（降速T1 exp，升速T2 d²）"""
      penalty = 0.0
      speeds = params['SPEED']
      for i in range(1, len(speeds)):
          if speeds[i-1] > 0 and speeds[i] > 0:
              ratio = speeds[i] / speeds[i-1]
              if ratio < 1.0:
                  d = max(0.0, (1.0-ratio) - 0.05) / 0.05
                  penalty += apply_penalty(d, tier=1)   # T1 exp
              elif ratio > 1.0:
                  d = max(0.0, (ratio-1.0) - 0.20) / 0.20
                  penalty += apply_penalty(d, tier=2)   # T2 d²
      return penalty

  ═══════════════════════════════════════════════════
  Level 2: 仿真后评价 evaluate_constraints(vals, speeds, ort)
  ═══════════════════════════════════════════════════

  统一的归一化偏差计算：
    def normalized_deviation(value, spec):
        if spec['type'] == 'unilateral_upper':
            return max(0, value - limit) / norm
        elif spec['type'] == 'unilateral_lower':
            return max(0, limit - value) / norm
        elif spec['type'] == 'bilateral':
            if value < lo:  return max(0, lo-value) / norm_lo
            if value > hi:  return max(0, value-hi) / norm_hi
            return 0.0

  分级惩罚函数：
    def apply_penalty(d, tier):
        if d <= 0: return 0.0
        if tier == 1: return W_T1 × (exp(k × d) - 1)    # k=2.0, W_T1=1.0
        if tier == 2: return W_T2 × d²                    # W_T2=0.8
        if tier == 3: return W_T3 × d²                    # W_T3=0.5

  约束评价（spec 驱动）：
    def evaluate_constraints(vals, speeds=None, ort=None):
        penalty = 0.0
        for spec in CONSTRAINT_SPECS:
            p = _eval_spec(spec, vals, speeds, ort)
            penalty += p
        return penalty, details

  14个CONSTRAINT_SPECS定义约束元数据（名称、层级、数据源、阈值、归一化因子），
  _eval_spec根据spec自动选择数据源（vals/speeds/ort）和约束类型。

  已删除：S7(TS)、S8(Z)、H1、H6、H7、B1-B6、W_BONUS、compute_total_score(feasible,...)

  ═══════════════════════════════════════════════════
  完整评价管线（stelmor_batch_cost_function in PO.py）
  ═══════════════════════════════════════════════════

  for each candidate solution x ∈ R^21:
      1. params = extract(x) → {SPEED[10]}
      2. pre_penalty = pre_check_bounds(params)           # 速比约束
      3. state_data = run_single_simulation(params)       # sim_T
      4. vals = extract_from_state_data(state_data)       # 提取 cr_pearl 等
      5. penalty, details = evaluate_constraints(vals, speeds, ort)
      6. score = pre_penalty + penalty                    # 纯惩罚

  注：已移除 ML 预测步骤（predict_Ts），无需温度重采样。

3.12 硬约束在候选解生成中的嵌入（PO.py _repair_speeds）

  除 H4（珠光体转变量 ≥ 85%，需仿真后判断）外，其余硬约束均在候选解生成阶段
  直接嵌入，使所有候选解自动满足：

    H1: ORT ∈ [890, 940]         → 由搜索空间 lb/ub 保证
    H6: SPEED ∈ [0.5, 1.6] m/s   → 由搜索空间 lb/ub 保证
    H7: 相邻段辊速比 ≤ 1.5       → 由 _repair_speeds() 修复函数保证

  H7 修复逻辑（级联约束）：

    从 SPEED1 向后级联，每段被限制在 [prev/1.5, prev*1.5] 内：

    def _repair_speeds(X, lb, ub):
        X_r = np.clip(X, lb, ub)
        for i in range(2, 11):          # SPEED2 ~ SPEED10 (索引 2~10)
            prev = X_r[i - 1]
            lo = prev / 1.5
            hi = prev * 1.5
            if X_r[i] < lo or X_r[i] > hi:
                X_r[i] = np.clip(X_r[i], lo, hi)
        return np.clip(X_r, lb, ub)

  调用点（PO.py puma_optimize）：
    - 种群初始化：X_init[i] = _repair_speeds(X_init[i], lb, ub)
    - 探索阶段：  candidates[i] = _repair_speeds(z, lb, ub)
    - 开发阶段：  new_sol.X = _repair_speeds(_clamp_to_bounds(...), lb, ub)

  设计理由：与 pre_check_bounds 拒绝不可行解相比，修复策略将不可行解投影到最近
  可行域，避免了浪费仿真计算在注定被拒绝的候选解上。SPEED1 保持原值作为锚点，
  后续速度逐个适配，保证整条辊道速度曲线的物理可实现性。

═══════════════════════════════════════════════════════════════
四、Step 2：工艺参数 → 力学性能的影响分析
═══════════════════════════════════════════════════════════════

4.1 工艺参数-性能关系链

  可调工艺参数                中间状态              最终性能
  ─────────────────────────────────────────────────────────
  ORT（吐丝温度）         → 奥氏体晶粒度         → Rp, TS
  SPEED 1-10（辊道速度）  → 冷却时间 / 线环间距  → TS, EXT
  FAN 1-6（风机开度）     → 冷却速率 / 温度曲线   → TS, EXT

4.2 量化方法

  方法 1（优先）：Morris 全局敏感性分析
    - 在 17 维工艺参数空间随机采样 ~200 组
    - 调用 sim.run_full_simulation() → 提取温度曲线特征
    - 调用 data_driven_model 预测 TS，计算 EXT
    - 计算每个参数的 Morris 灵敏度指数 μ* 和 σ
    - 筛选出显著的前 5-8 个关键参数

  方法 2：SHAP 值分析
    - 用训练好的 Transformer 模型对仿真数据做模型解释
    - 识别哪些温度时间段对 TS 预测影响最大
    - 反推：影响大的时间段 → 对应的辊道段 → 该段风机/速度是关键

  方法 3：替代模型
    - 训练轻量 MLP: [SPEED, FAN, ORT] → [冷却速率, 相变温度, 温差]
    - 比 sim_T 快 1000 倍，可在优化器内层使用

═══════════════════════════════════════════════════════════════
五、Step 3：评分方法 + 优化算法
═══════════════════════════════════════════════════════════════

5.1 评分函数设计

  Score(x) = w1 × constraint_penalty(x)      # 约束惩罚（硬+软）
           + w2 × (1 - norm_TS_score(x))      # TS 偏离目标 1050 MPa
           + w3 × (1 - norm_EXT_score(x))     # EXT 偏离目标
           + w4 × uniformity_penalty(x)        # 搭接/非搭接温差
           - w5 × expert_bonus(x)              # 专家知识奖励

  硬约束（不可违反）：
    - 珠光体温区冷却速率 ∈ [8, 15] °C/s
    - 珠光体转变量 ≥ 0.85

  软约束（惩罚项）：
    - 搭接/非搭接 max ΔT > 20 °C → quadratic penalty
    - 末端冷却速率 > 5 °C/s → penalty

5.2 推荐优化算法

  ┌──────────────────────────┬──────────────────┬──────────────────────┐
  │ 场景                     │ 推荐算法         │ 理由                 │
  ├──────────────────────────┼──────────────────┼──────────────────────┤
  │ 评估昂贵（需 sim_T）     │ Bayesian Opt     │ 100-500 次评估收敛   │
  │ 有廉价替代模型           │ CMA-ES           │ 10-20 维连续高效     │
  │ 需要可解释性             │ PSO              │ 已有成熟实现         │
  │ 多目标（TS + EXT）       │ NSGA-II / MOTPE  │ 输出 Pareto 前沿     │
  └──────────────────────────┴──────────────────┴──────────────────────┘

═══════════════════════════════════════════════════════════════
六、关键注意点
═══════════════════════════════════════════════════════════════

  1. 缺失 Z 的补偿：评分中用 EXT 替代 Z，并辅以 TS/EXT 组合判断、
     Rp/TS 屈强比、温度曲线均匀性等间接指标

  2. 误差分离策略：以 sim_T 物理输出（冷却速率、相变温度、温差）
     为主约束/评分，data_driven 的 TS/EXT 预测仅作辅助参考

  3. 验证闭环：最优解应能通过 sim_T 重现，且温度曲线落在专家知识
     定义的合格区间内。如果最优解违反约束但评分高 → 调整权重

  4. 可操作性约束：SPEED 和 FAN 有工业离散档位，优化结果需映射到
     最近可操作档位后验证

  5. sim_T 精度优先：先运行 change_parameter_82A.py 修正仿真参数，
     缩小 sim_T 与实测的偏差，再在此基础上优化工艺

═══════════════════════════════════════════════════════════════
七、参考文献
═══════════════════════════════════════════════════════════════

  [1]  Kazeminezhad M, Karimi Taheri A. The effect of controlled cooling
       after hot rolling on the mechanical properties of a commercial high
       carbon steel wire rod. Materials & Design, 2003, 24(6): 415-421.
       DOI: 10.1016/S0261-3069(03)00095-5

  [2]  Li Zhanwei et al. Phase transformation behavior of high strength
       cord steel LX82ACr during dynamic continuous cooling. Heat Treatment
       of Metals, 2023, 48(9): 214-219.

  [3]  82A钢高速线材控制冷却工艺（WLX82A CCT实测）. 热加工行业论坛 /
       Mysteel, 2015. https://tech.mysteel.com/15/1129/20/94763D1253CC23C9.html

  [4]  朱兴安 (宝钢). 一种以温度为直控参数的高速线材斯太尔摩线冷却方法.
       CN102151704A / CN102151704B.

  [5]  55SiCrA弹簧钢线材的Stelmor控冷方法. CN102747210A / CN102747210B.

  [6]  Morgan Construction Company. Apparatus for controlled cooling hot
       rolled steel rod in direct sequence with rod mill. US Patent 3,940,967.

  [7]  T/JSGT 006-2021 胎圈钢丝及钢帘线用盘条. 江苏省钢铁行业协会.
       https://www.ttbz.org.cn/StandardManage/Detail/50764

  [8]  Deng T.W. et al. Finite Element Simulation and Parameter Optimization
       of SWRH82B Wire Rod in Stelmor Cooling Process. J. Mater. Eng. Perform.,
       2025, 34, 11212-11225. DOI: 10.1007/s11665-024-09898-2

  [9]  Nippon Steel & Sumitomo Metal. High-Carbon Steel Wire Rod with
       Excellent Wire Drawability. US Patent 2017/0321309 A1.

  [10] 王伯文, 杭乃勤. 82A钢高速线材控制冷却工艺的优化. 特殊钢, 2009,
       30(6): 14-16. （武汉科技大学硕士论文，2009）

  [11] 刘力哲, 余万华, 刘新鹏等. C82DA帘线钢相变规律及动力学模型.
       材料热处理学报, 2014, 35(2): 220-224.
       DOI: 10.13289/j.issn.1009-6264.2014.02.040
       → JMAK Avrami 指数 n=1.8214; lnk-T 二次关系式; Ac₁=732°C; Accm=759°C

  [12] GB/T 27691-2017 钢帘线用盘条（替代 GB/T 27691-2011）.
       https://www.spc.org.cn/online/5b668ff55eb3c7295dc66d07211e4055.html

  [13] YB/T 169-2014 高碳钢盘条索氏体含量金相检测方法（替代 YB/T 169-2000）.

  [14] 82B高碳钢盘条连续冷却转变曲线的测定及其相变规律. 理化检验-物理分册.
       → Ac₁=731°C, Accm=780°C（82B实测，82A可互参）

  [15] SWRH82A+Cr热轧盘条生产工艺实践. 金属制品, 2015.
       → 实际生产：索氏体≥85%, TS=1196-1221 MPa, Z=39-42%

  [16] Chu Feng et al. Phase transformation behavior of high-carbon cord
       steel during cooling. 材料热处理学报.
       → 珠光体 nose ~625°C; 贝氏体 below ~550°C

  [17] Zhao Qianlong, Su Fuyong, Li Bin et al. Temperature distribution
       analysis and mechanical property prediction of wire loops in the
       Stelmor air-cooling system. Case Studies in Thermal Engineering,
       2024, p.104536.

  [18] 鞍钢. 一种超高碳帘线钢盘条中网状渗碳体的控制方法. CN105256119A.
       → 相变前冷速 10-50 °C/s, 相变期间 3-10 °C/s

  [19] Cause analysis and improvement of tensile strength fluctuation in
       the same circle of large size SWRS82B steel wire rod. Heat Treatment
       of Metals, 2024, 49(1): 273-278.
       → 含V钢：冷速≥3°C/s + T<550°C → 贝氏体

  [20] Primetals Technologies. Morgan Stelmor® Controlled Cooling Conveyor
       for Wire Rod Mills.
       https://www.primetals.com/en/portfolio/solutions/long-rolling/
       wire-rod-mill/morgan-stelmor-controlled-cooling-conveyor/

  [21] 斯太尔摩线. 百度百科.
       https://baike.baidu.com/item/斯太尔摩线/2049931
       → 三种类型（标准/缓冷/延迟冷）辊速范围：0.05-1.30 m/s

  [22] JIS G 3506:2004 High carbon steel wire rods (SWRH82A/B 成分标准).
